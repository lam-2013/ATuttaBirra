<% #Provide the page title in a symbol to application.html.erb to avoid duplication %>
<% provide :title, 'Cerca' %>

<h1 id="arancione">Cerca</h1>

<fieldset>
<div id="ricerca">
<p id="ricerca">Ricerca tramite:</p>

<% # text_field_tag: the first parameter is the id/name of the input tag; the second is the value of the tag %>

<%= form_tag search_users_path, method: 'get' do %>
<%# form_for @search do |f| %>

    <div id="ricercautenti">
   <p id="ricercautenti">
<%= label_tag :utenti %>
</p>
<p id="ricercautenti">
 <%= text_field_tag :search, params[:search], class: 'search-query', placeholder: 'Search' %>
</p>
<p id="ricercautenti">
<%= submit_tag "Search",  class: 'btn btn-large btn-primary' %>
</p>
  </div>


    <div id="ricercagruppi">
<p id="ricercagruppi">
<%= label_tag :gruppi %>
</p>
<p id="ricercagruppi">
<%= text_field_tag :search, params[:search], class: 'search-query', placeholder: 'Search'  %>
</p>
<p id="ricercagruppi">
<%= submit_tag "Search",  class: 'btn btn-large btn-primary' %>
</p>
  </div>


    <div id="ricercatag">
<p id="ricercatag">
<%= label_tag :tag %>
</p>
<p id="ricercatag">
<%= text_field_tag :search, params[:search], class: 'search-query', placeholder: 'Search' %>
</p>
<p id="ricercatag">
<%= submit_tag "Search",  class: 'btn btn-large btn-primary'  %>
</p>
   </div>

<% end %>

</div>

</fieldset>

