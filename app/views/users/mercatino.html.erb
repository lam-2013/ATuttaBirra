<% provide :title, 'Mercatino' %>

<%  @user = User.find(params[:id])  %>

<% if @user == current_user%>
<div>
  <%= image_tag ("Mercatino.png") %>
</div>

<% else %>
<div id="banner">

  <% if current_user || (@user) %>
      <%  @user = User.find(params[:id])  %>
  <% end %>
  <%= render 'shared/infoutente' %>
</div>

<div class="row">
  <aside class="span4">
    <section>
      <%= render 'shared/barra_amico' %>
    </section>
  </aside>

  <div class="span8">
    <%= image_tag ("Mercatino.png") %>
  </div>

</div>
<% end %>
