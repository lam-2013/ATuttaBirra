<li>
<%= gravatar_for user, size: 52 %>
<%= link_to user.name, user %>
<p id="risultatiricerca">
<%= user.chisei %>
 - Punteggio: <%= user.punteggio %>
<br>
Tag dell'utente:
<%= link_to user.tag1, {:controller => "users", :action => "search_tag", :search_tag => user.tag1}%>
<%= link_to user.tag2, {:controller => "users", :action => "search_tag", :search_tag => user.tag2}%>
<%= link_to user.tag3, {:controller => "users", :action => "search_tag", :search_tag => user.tag3}%>

</p>
<% if current_user.admin? && !current_user?(user) %>
  <% # since browsers cannot send DELETE request, Rails fakes them with javascript... %>
  | <%= link_to 'delete', user, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
</li>