<% #Provide the page title in a symbol to application.html.erb to avoid duplication %>
<% provide :title, 'Utenti trovati' %>

<h1><%= page_entries_info @users %></h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
</ul>


<%= will_paginate %>